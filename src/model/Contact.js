export default class Contact {
  constructor(phone, lastname, firstname, email, profile, gravatar, id) {
    this.phone = phone,
    this.lastname = lastname,
    this.firstname = firstname,
    this.email = email,
    this.profile = profile,
    this.gravatar = gravatar,
    this.id = id;
  }
}
